UPDATE channel_event
SET event = event->'Channel'->'OpenChannel'
WHERE event_type = 0
and imported = false;

UPDATE channel_event
SET event = event->'Channel'->'ClosedChannel'
WHERE event_type = 1
and imported = false;
